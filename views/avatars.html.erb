<% server = "http://#{request.host_with_port}" %>

// occupyinter.net audio
if (typeof(occupy_internet_audio_src) == 'undefined') {
  var occupy_internet_audio_src = '<%= server %>/crowd.mp3';
}

// occupyinter.net protesters
if (typeof(occupy_internet_protesters) == 'undefined') {
  var occupy_internet_protesters = [

    // Put your avatars in _avatars.html.erb!
    <%= erb :'_avatars' %>

  ];
}